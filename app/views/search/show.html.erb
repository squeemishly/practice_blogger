<ul>
  <% if @found_articles %>
    <% @found_articles.each do |article| %>
      <li><%= link_to article.title, article_path(article.id) %> written by <%= article.user.username %></li>
    <% end %>
  <% end %>
</ul>
